CREATE TABLE users(uid INTEGER, password VARCHAR(20), username VARCHAR(20), coins INTEGER, betNum INTEGER, betEO INTEGER, betNumAmount INTEGER, betEOAmount INTEGER);
#EO (bett on odd or even), Num (the number that has been bet on)